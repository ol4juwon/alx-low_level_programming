#include "search_algos.h"

/**
 * linear_search - search for a vlaue in an array
 * @array: point to array to search
 * @size: size of array 
 * @value: item to search
 * Return: -1 if not found or
 * index if found
 */

int linear search(int *array, size_t size, int value)
{
if (array == NULL)
return (-1);

}
